name: Build Release

on:
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
      pull-requests: read
      actions: write
    uses: Yueby/shared-workflows/.github/workflows/unity-package-release.yml@main
    with:
      package-name: ${{ vars.PACKAGE_NAME }}

  trigger-other:
    needs: release
    if: ${{ vars.TARGET_ACTION != '' }}
    permissions:
      actions: write
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Repository Workflow
        run: |
          IFS=':' read -r repo workflow <<< "${{ vars.TARGET_ACTION }}"
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ github.token }}" \
            https://api.github.com/repos/${{ github.repository_owner }}/${repo}/actions/workflows/${workflow}/dispatches \
            -d '{"ref":"main","inputs":{}}'